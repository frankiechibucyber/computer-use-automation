.venv/
__pycache__/
*.pyc
.env
.pytest_cache/

# Generated capability registry — regenerated by `cua discover`; each file carries a
# machine-specific target path in its provenance, so it isn't committed. The example artifact
# ships inside /evidence/ instead (which the brief requires as a deliverable).
catalogs/capabilities/*.artifact.json

# Working / process documents — not part of the submission.
CUA-understanding.md
CUA-scoring.md
_scratch/
